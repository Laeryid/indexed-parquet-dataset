# API Reference

This section provides auto-generated documentation for the core classes and methods in `indexed-parquet-dataset`.

## IndexedParquetDataset

The main class used for indexing and accessing Parquet datasets.

::: indexed_parquet.dataset.IndexedParquetDataset
    options:
      members:
        - from_folder
        - shuffle
        - train_test_split
        - select
        - limit
        - map
        - alias
        - rename
        - cast
        - filter
        - select_columns
        - to_parquet
        - iter_batches

## SchemaMapper

Handles column name mapping and transformations.

::: indexed_parquet.schema.SchemaMapper

## BaseIndex

Base class for dataset indexes.

::: indexed_parquet.indexer.BaseIndex
